<!-- /templates/page.liquid -->
<div class="grid">
  <div class="container exclusives">

    <header class="page-title container-wrap">
      <h1>{{ page.title }}</h1>
     
    </header>

    <div class="grid">
      <div class="grid__item large--one-whole">
        <div class="rte rte--nomargin rte--indented-images">
          {{ page.content }}
        </div>
      {% section 'exclusive-row' %}
        {% section 'everyday-offers' %}
      </div>
      
    </div>

  </div>
</div>
